schema: http://localhost:3000/api/graphql
documents: src/**/!(*.d).{ts,tsx}
generates:
  src/lib/graphql/react-query-typegen.tsx:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-query
    config:
      fetcher: graphql-request
