query TeamBySlug($slug: String!) {
    team(slug: $slug) {
        id
        name
        city
        established
        logo
        colorScheme {
            primary
            secondary
        }
    }
}